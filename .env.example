# Passed to vroom as -a / -p (same role as routingServers.osrm.car in vroom-express config.yml).
# Compose sets these to osrm:5000; for local OSRM on the host, defaults match below.
VROOM_OSRM_HOST=localhost
VROOM_OSRM_PORT=5000
